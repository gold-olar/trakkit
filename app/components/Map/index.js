import React from "react";
import { View } from "react-native";
import { MapView } from "react-native-maps";

const Map = () => {
  return (
    <View>
      <MapView />
    </View>
  );
};

export default Map;
